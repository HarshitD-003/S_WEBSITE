// Get the CSS rule
const styleSheet = document.styleSheets[0];
const rule = styleSheet.cssRules[0];

// Define an array of four different URLs
const imageUrls = [
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg'
];

// Iterate through the URLs and change the CSS rule
imageUrls.forEach((url, index) => {
  const className = `.el:nth-child(1).el__bg${index + 1}:before`;
  const newRule = `${className} {
    transition-delay: 0.2s;
    background-image: url(${url});
  }`;

  styleSheet.insertRule(newRule, styleSheet.cssRules.length);
});

const styleSheet2 = document.styleSheets[0];
const rule2 = styleSheet.cssRules[0];

// Define an array of four different URLs
const imageUrls2 = [
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg'
];

// Iterate through the URLs and change the CSS rule
imageUrls2.forEach((url2, index2) => {
  const className2 = `.el:nth-child(2).el__bg${index2 + 1}:before`;
  const newRule2 = `${className2} {
    transition-delay: 0.2s;
    background-image: url(${url2});
  }`;

  styleSheet2.insertRule(newRule2, styleSheet2.cssRules.length);
});

const styleSheet = document.styleSheets[0];
const rule = styleSheet.cssRules[0];

// Define an array of four different URLs
const imageUrls = [
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg'
];

// Iterate through the URLs and change the CSS rule
imageUrls.forEach((url, index) => {
  const className = `.el:nth-child(1).el__bg${index + 1}:before`;
  const newRule = `${className} {
    transition-delay: 0.2s;
    background-image: url(${url});
  }`;

  styleSheet.insertRule(newRule, styleSheet.cssRules.length);
});

const styleSheet = document.styleSheets[0];
const rule = styleSheet.cssRules[0];

// Define an array of four different URLs
const imageUrls = [
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg'
];

// Iterate through the URLs and change the CSS rule
imageUrls.forEach((url, index) => {
  const className = `.el:nth-child(1).el__bg${index + 1}:before`;
  const newRule = `${className} {
    transition-delay: 0.2s;
    background-image: url(${url});
  }`;

  styleSheet.insertRule(newRule, styleSheet.cssRules.length);
});

const styleSheet = document.styleSheets[0];
const rule = styleSheet.cssRules[0];

// Define an array of four different URLs
const imageUrls = [
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg'
];

// Iterate through the URLs and change the CSS rule
imageUrls.forEach((url, index) => {
  const className = `.el:nth-child(1).el__bg${index + 1}:before`;
  const newRule = `${className} {
    transition-delay: 0.2s;
    background-image: url(${url});
  }`;

  styleSheet.insertRule(newRule, styleSheet.cssRules.length);
});

const styleSheet = document.styleSheets[0];
const rule = styleSheet.cssRules[0];

// Define an array of four different URLs
const imageUrls = [
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg'
];

// Iterate through the URLs and change the CSS rule
imageUrls.forEach((url, index) => {
  const className = `.el:nth-child(1).el__bg${index + 1}:before`;
  const newRule = `${className} {
    transition-delay: 0.2s;
    background-image: url(${url});
  }`;

  styleSheet.insertRule(newRule, styleSheet.cssRules.length);
});

const styleSheet = document.styleSheets[0];
const rule = styleSheet.cssRules[0];

// Define an array of four different URLs
const imageUrls = [
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg',
  '../sankalpimg/jan5.jpg'
];

// Iterate through the URLs and change the CSS rule
imageUrls.forEach((url, index) => {
  const className = `.el:nth-child(1).el__bg${index + 1}:before`;
  const newRule = `${className} {
    transition-delay: 0.2s;
    background-image: url(${url});
  }`;

  styleSheet.insertRule(newRule, styleSheet.cssRules.length);
});
